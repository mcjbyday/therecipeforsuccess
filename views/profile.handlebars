{{#if meals.length}}
                            <div class="col-md-6 meal-list">
                                <h3>Your Posts:</h3>
                                {{#each meals as |meal|}}
                                <div class="row mb-2">
                                <div class="col-md-6">
                                    <h4><a href="/meal/{{meal.id}}">{{meal.title}}</a></h4>
                                </div>
                                        {{!-- <div class="col-md-3">
                                        <button id="edit" type="submit" class="btn" data-id="{{meal.id}}"><a class="link" href="/edit/{{meal.id}}">EDIT</a></button> --}}
                                    {{!-- <button id="edit" class="btn btn-sm btn-alert" data-id="{{meal.id}}">EDIT</button> --}}
                                {{!-- </div>
                                <div class="col-md-3">
                                    <button id="delete" class="btn btn-sm btn-danger" data-id="{{meal.id}}">DELETE</button>
                                </div> --}}
                                {{!-- </div> --}}
                                {{/each}}
                            </div>
                            {{/if}}
                            </div>